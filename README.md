# nanoDiffusionCodex

Small diffusion model project for coding-focused generation and editing.

## Current Status
This repository now contains:
- A concrete execution plan in `plan.md`.
- Initial source/package scaffolding under `src/nano_diffusion/`.
- Placeholder CLI scripts for train/eval/infer in `scripts/`.
- Basic documentation and smoke test scaffolding.

## Quick Start
```bash
python scripts/train.py
python scripts/eval.py
python scripts/infer.py
```

## Repository Layout
- `plan.md` — roadmap and milestones.
- `docs/architecture.md` — initial module boundaries.
- `src/nano_diffusion/` — core package skeleton.
- `scripts/` — executable entrypoints.
- `tests/` — test scaffolding.
